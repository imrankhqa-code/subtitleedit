Yes, hello. Hello, Pradeep. Good afternoon. How are you doing? Yes, I'm doing well. Thank you for taking the time to meet with me today. Sure, yeah. Yes, I'll get started.
 I have seven years of experience. I've been working with Aden Pro as a cloud DevOps engineer. You know, for the last two and a half years, I've been working in cloud, you know, across different clouds like, you know, Azure, Google Cloud, and Kubernetes. You know, currently, you know, I was working to migrate applications from the data center to the cloud, which includes, you know, both migration and modernization. The six hours of it, including re-host, re-factor, re-platform, and re-architect. This was predominantly on Azure migration using virtual machines, virtual machine scale sets, AKS, app functions, service bus, block containers, and Azure SQL. Currently, I'm working on an implementation project for For system of engagement using Google kubernetes engine and end-to-end devops. My day-to-day responsibilities includes as a devops lead. I'm responsible for using azure pipelines to develop the ci/cd Pipelines and then, you know, I use Terraform for infrastructure as code, GitHub for the source repository, and then Docker for creating images, container images, and then Kubernetes for container orchestration. I have good experience and knowledge with Kubernetes. Yes. 
Yes, yes, I have Terraform knowledge, yes. I worked extensively in Terraform, right? Yeah, yeah. So if you look at the four major constructs within Terraform, it is Terraform unit, Terraform validate, Terraform plan, and Terraform apply. If you look at Terraform unit, it is to basically initialize the working directory, which means that it initializes, it downloads the plugins from the required providers, initializing the modules, and then specifying the backend to be used to store the state files. And then validate is basically to validate all the TF files sitting in the working directory. And then we have Terraform plan, which is to validate all the TF file configuration files against the state and determining, you know, what changes has happened. Very specifically, whether it's going to be, whether a source is going to be created, updated or destroyed. And then we have Terraform apply, which is basically to apply the infrastructure, apply the changes and the infrastructure gets updated. And the state file gets updated, too. This is basically the most important commands within Terraform. Yes, I have done the complete CI/CD pipeline. So, yeah, my complete CI/CD pipeline starts with both the build and release pipeline. So from a build pipeline standpoint, most of the files, most of the pipeline that we have developed is basically docker pipeline. Basically, yaml pipeline for creating docker images and deploying it into kubernetes. 
The reason behind it is our applications are predominantly developed in .NET core and as developed as microservices and deployed as container images. So if you look at how our CI-CD pipeline starts, our CI starts with the trigger into a feature branch. So whenever there is a pull request or a check-in happens into the feature branch, the pipeline gets triggered. And then the most important things is setting up the image. We predominantly use only the Microsoft hosted agent, which is basically Ubuntu latest in this scenario. And then the multiple stages within the pipeline, as I said, both the build and the release pipeline. From a build pipeline standpoint, the most important stage and the task within the stage is basically the Docker task, which is basically the command is build and push, which uses the Docker file that is available within the microservice to build the image. And once we build the image, we push the image into the Azure Container Registry. We use the service connections to connect to GitHub, Azure Container Registry, and then to Azure as well. And then after we do the Docker task, we do the SonarQube, which is for static code analysis. And then we have the Veracode for SAST and DAST. which is basically the security testing. The CVEs are identified. And then once the image has been created and put it into ECR, and then the security testing is completed, we have a Helm chart, which is basically the package manager for Kubernetes, which creates a package. which is basically a helm release. We have the template defined, which is basically your deployment.yaml, your services.yaml, ingress.yaml, and then the HPA settings, the config map secrets. All of them is going to be part of the helm release, and then it is stored as an artifact. This is when the CI completes and then the CD gets triggered. Within CD, as part of the continuous deployment, we have three stages, which is basically the development stage, test stage, and the production stage. The development stage is basically to deploy the application into the development environment, the development Kubernetes environment cluster. We use the Helm release to deploy it into the Kubernetes cluster. Then we have the automated regression test cases that runs to test the code that is being deployed. Then we have manual checks and gates in place where we validate the SonarQube output, the testing team validates the CVEs from Veracode, And then the regression test results, depending upon the criticality of the issues that has been identified. We have manual checks and gates in place to. An approval happens to move it into the test environment. After it goes into the test environment. It goes through the through the system integration testing end to end testing and then user acceptance testing. And then. We have manually, we don't do a continuous deployment into production, but we use the Helm release to deploy the same Helm release to deploy this into the production environment. So this is the approach that we have followed for all the microservices. We predominantly use the YAML pipeline to do that. Yes, I have worked in Kubernetes. I've worked extensively in Kubernetes, yes. So, if you look at the architecture of Kubernetes, Kubernetes is basically a, basically architecture is based on master nodes and worker nodes. If you look at the master node, master node has four major components, which is basically kube-api-server, scheduler, controller, and hcd-server, etcd-server. So if you look at the API server, which is basically the front end of Kubernetes cluster, the scheduler is responsible for scheduling all the parts. The controller is responsible for orchestration based on the different resources that is going to be created. And then we have the HCD server, which is a key value pair. And then if you look at the worker nodes, the worker nodes basically have the kubelet and the kubeproxy. The kubelet is responsible, is basically an agent that is running in the worker node, which listens for the kube api server. And then we have the kube proxy, which is responsible for part-to-part communication using the services, the service discovery part of it. These are the major components within the Kubernetes cluster. The last one which has the container runtime, which is the container D that runs on across all the nodes. These are the major components of Kubernetes. Okay, sure. Thank you. Thank you for taking the time to meet with me. I love the interview. Thank you. Have a great day.
 

